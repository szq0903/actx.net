! function (a) {
	a.fn.scrollLoading = function (b) {
		var e, f, c = {
				attr: "data-url",
				_src: "data-src",
				container: a(window),
				callback: a.noop
			},
			d = a.extend({}, c, b || {});
		d.cache = [], a(this).each(function () {
			var b = this.nodeName.toLowerCase(),
				c = a(this).attr(d["attr"]) || a(this).attr(d["_src"]),
				e = {
					obj: a(this),
					tag: b,
					url: c,
					src: c
				};
			d.cache.push(e)
		}), e = function (b) {
			a.isFunction(d.callback) && d.callback.call(b.get(0))
		}, f = function () {
			var b = d.container.height();
			contop = d.container.get(0) === window ? a(window).scrollTop() : d.container.offset().top, a.each(d.cache, function (a, c) {
				var h, i, d = c.obj,
					f = c.tag,
					g = c.url;
				d && (h = d.offset().top - contop - 100, i = h + d.height(), (h >= 0 && b > h || i > 0 && b >= i) && (g ? "img" === f || "iframe" === f ? e(d.attr("src", g)) : d.load(g, {}, function () {
					e(d)
				}) : e(d), c.obj = null))
			})
		}, f(), d.container.bind("scroll", f)
	}
}(jQuery);
